# USE_SPECIFIERS { _post_synaptic }

import numpy as np

{% for line in code_lines %}
{{line}}
{% endfor %}

_target_var[:] = np.array([np.sum(_synaptic_var[_post_syns.data])
                         for _post_syns in _post_synaptic])