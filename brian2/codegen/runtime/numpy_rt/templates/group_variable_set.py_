# USES_VARIABLES { _group_idx, offset }
if offset != 0:
    i = _group_idx - offset
else:
    i = _group_idx
_idx = _group_idx
_vectorisation_idx = _idx
{% for line in code_lines %}
{{line}}
{% endfor %}
