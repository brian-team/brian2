# USES_VARIABLES { _synaptic_post }
# ITERATE_ALL { _idx }
{% if _get_target_var_name is defined %}
{% set _target_var = _get_target_var_name() %}
{% endif %}

import numpy as np

{% for line in code_lines %}
{{line}}
{% endfor %}


# We write to the array, using the name provided as a keyword argument to the
# template
{{_target_var}}[:] = np.bincount({{_synaptic_post}},
                                 minlength=len({{_target_var}}),
                                 weights=_synaptic_var)